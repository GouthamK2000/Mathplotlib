import matplotlib.pyplot as plt
import numpy as np

xpoints = np.array([0, 6])   # x poiints of the line
ypoints = np.array([0, 250]) # y points of the line

plt.plot(xpoints, ypoints)  # variables should be the same as  assigned as earlier
plt.show()   # this displayes our graph


#Any number of numbers  can be given inside the x and y points.They must be equal in nos.